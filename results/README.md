# Results

This directory stores high-level, not sensitive outputs generated from the
healthcare counseling evaluation pipeline.

Typical contents include:
- Plots comparing models (e.g., sentiment, readability)
- Summary tables aggregating scores across models

Full LLM responses and detailed processed datasets are **not** stored here.
They are generated locally in `data/processed/` and excluded from version control.
